{% extends '::base.html.twig' %}

{% block body -%}
    <h1>西南大学广州校友会校友名录资料查询录入</h1>
    <h4 style="color: red">{{ errorHint }}</h4>
    <p>请输入真实姓名和手机号码最后4位查询收录情况。您也可以直接“新建联系人”。</p><p>&nbsp;</p>
    <form class="form-horizontal" style="width: 300px; display: block;text-align: center;" action="{{ path('search_result') }}" method="post" >
        <div class="form-group col-lg">
            <input type="text" name="nameSearch" class="form-control " id="searchText" placeholder="请输入真实姓名" value="{{ name }}"/>
        </div>
        <div class="form-group col-lg">
            <input type="text" name="phoneSearch" class="form-control " id="searchText" placeholder="请输入手机号码最后四位数字" value="{{ phone }}"/>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary btn-sm-5" style="display: inline-block;">查询</button>
            <span style="margin-right: 50px;">&nbsp;</span>
            {% if name %}
                <a href="{{ path('search_new', { 'name': name }) }}" class="btn btn-primary btn-sm-5">
                    新建联系人
                </a>
            {% else %}
                <a href="{{ path('search_new', { 'name': '' }) }}" class="btn btn-primary btn-sm-5">
                    新建联系人
                </a>
            {% endif %}
        </div>
    </form>

    {% if entities %}
        <script>alert("{{ entities }}");</script>
    {% endif %}
{% endblock %}

